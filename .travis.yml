language: java
script:
- mvn install
os: linux
cache:
    directories:
        - $HOME/.m2
git:
    depth: 1
    submodules: false
jdk:
- oraclejdk17
- openjdk17
sudo: false
branches:
    only:
    - master
